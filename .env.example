CLOUDFLARE_ACCOUNT_ID=96cf3886ebd2c63d32d8455b9667b46c
HOMEOS_DEV_PORT=8787
HOMEOS_SMOKE_WEB_PORT=13100

# Production secrets are never stored here. HOMEOS_MCP_TOKEN is set with
# `wrangler secret put`; the original API token is held in macOS Keychain and
# Wrangler OAuth is preferred for provisioning and deployment.
