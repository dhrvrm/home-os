CLOUDFLARE_ACCOUNT_ID=96cf3886ebd2c63d32d8455b9667b46c
HOMEOS_DEV_PORT=8787
HOMEOS_SMOKE_WEB_PORT=13100

# Worker secrets are never stored here. BETTER_AUTH_SECRET, GOOGLE_CLIENT_ID,
# and GOOGLE_CLIENT_SECRET are set with `wrangler secret put`. The Cloudflare
# API token remains in macOS Keychain; Wrangler OAuth is preferred for deploys.
